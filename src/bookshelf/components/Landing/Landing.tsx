import React from 'react';
import AddFirebaseConfig from "../AddFirebaseConfig/AddFirebaseConfig";

class Landing extends React.Component {
  render() {
    return (
      <main>
        <AddFirebaseConfig />
      </main>
    );
  }
}

export default Landing;